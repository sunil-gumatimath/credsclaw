services:
  auditor:
    build: .
    env_file:
      - .env
    environment: {}
    volumes:
      # Persist checkpoint/results/logs on the host.
      - ./docker-output:/work
    # Default CMD is --help, you can override it, e.g.:
    # command: ["--mode", "code", "--providers", "all"]
